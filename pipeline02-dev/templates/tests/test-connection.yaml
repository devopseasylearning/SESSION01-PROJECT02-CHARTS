apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pipeline02-dev.fullname" . }}-test-connection"
  labels:
    {{- include "pipeline02-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pipeline02-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
