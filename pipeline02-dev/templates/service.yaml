apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipeline02-dev.fullname" . }}
  labels:
    {{- include "pipeline02-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipeline02-dev.selectorLabels" . | nindent 4 }}
